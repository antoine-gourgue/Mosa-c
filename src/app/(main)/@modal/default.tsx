/**
 * Default render for the modal parallel slot: nothing is shown until an
 * intercepting modal route (the pin detail overlay) is active.
 *
 * @returns Null.
 */
export default function ModalDefault(): null {
  return null;
}
